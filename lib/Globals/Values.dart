library service_app.values;

import 'package:flutter/material.dart';

// Border values
double boxborderRadius = 5.0;
double imageHeightStandard = 60;
const double paddingFromWalls = 10.0;
const double paddingFromScreen = 20.0;
const double marginpaddingFromScreenHover = 4.0;
const double marginpaddingFromScreenFlat = 8.0;
const double marginForAvatarImages = 8.0;
const double dividerPadding = 6.0;

// Elevation
const double cardResting = 1.0;
const double cardHover = 6.0;
const double dialog = 16.0;
const double navigationDrawer = 16.0;
const double menus = 8;
const double switchWidget = 1;

// icon Values
double iconSize = 25.0;

// Font weights
FontWeight boldTextWeight = FontWeight.w500;
FontWeight normalTextWeight = FontWeight.w400;

// Theme Colors
// Black Theme Colors
Color themeBlackBackground = Color.fromRGBO(33, 33, 33, 1);
Color themeBlackPrimary = Color.fromRGBO(38, 50, 56, 1);
Color themeBlackPrimaryDark = Color.fromRGBO(23, 29, 33, 1);
Color themeBlackPrimaryLight = Color.fromRGBO(55, 71, 79, 1);

// Light Theme Colors
Color themeLightAppBackground = Color.fromRGBO(238, 238, 238, 1);
Color themeLightBackground = Color.fromRGBO(250, 250, 250, 1);
Color themeLightPrimary = Color.fromRGBO(41, 98, 255, 1);
Color themeLightPrimaryDark = Color.fromRGBO(13, 71, 161, 1);
Color themeLightPrimaryLight = Color.fromRGBO(68, 138, 255, 1);

// General Theme Colors
Color dafaultWhite = Color.fromRGBO(255, 255, 255, 1);
Color dafaultBlack = Color.fromRGBO(0, 0, 0, 1);

// Text Colors
Color errorRedText = Color.fromRGBO(176, 0, 32, 1.0);
Color normalBlackText = Color.fromRGBO(0, 0, 0, 0.87);
Color mediumBlackText = Color.fromRGBO(0, 0, 0, 0.6);
Color inactiveBlackText = Color.fromRGBO(0, 0, 0, 0.6);
Color disabledBlackText = Color.fromRGBO(0, 0, 0, 0.38);
Color helperBlackText = Color.fromRGBO(0, 0, 0, 0.6);

Color normalWhiteText = Color.fromRGBO(255, 255, 255, 0.87);
Color mediumWhiteText = Color.fromRGBO(255, 255, 255, 0.6);
Color inactiveWhiteText = Color.fromRGBO(255, 255, 255, 0.6);
Color disabledWhiteText = Color.fromRGBO(255, 255, 255, 0.38);
Color helperWhiteText = Color.fromRGBO(255, 255, 255, 0.6);
